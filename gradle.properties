# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.0.1
maven_group = info.rusty.mc
archives_base_name = mcwebsocketserver

# Dependencies are managed at gradle/libs.versions.toml
